
<div class="pure-menu pure-menu-open">
    <a class="pure-menu-heading">Contacts</a>
		<ul>
			<% @@rolodex.contacts.each do |contact| %>
				<li><%= contact.first_name %> <%= contact.last_name %> <%= contact.email %></li>
			<% end %>
		</ul>
</div>